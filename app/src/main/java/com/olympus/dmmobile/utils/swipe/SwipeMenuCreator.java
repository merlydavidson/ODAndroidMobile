package com.olympus.dmmobile.utils.swipe;

/**
 * Interface that define callback methods for creating a swipe menu
 * @version 1.2.0
 *
 */
public interface SwipeMenuCreator {

	void create(SwipeMenu menu);
}
